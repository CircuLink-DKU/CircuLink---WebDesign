# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.env
# Runtime upload directory only (repo root). Anchored with a leading slash so it
# does NOT also match the source module server/src/modules/uploads/.
/uploads/

# Local SQLite dev databases (never commit — may contain credentials/PII)
*.db
*.db-journal


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
